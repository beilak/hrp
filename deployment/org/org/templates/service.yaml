apiVersion: v1
kind: Service
metadata:
  name: {{ include "org.fullname" . }}
  labels:
    {{- include "org.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "org.selectorLabels" . | nindent 4 }}
